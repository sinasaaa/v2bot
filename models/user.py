# This file will define the database models using SQLAlchemy or SQLModel.
# For now, it's just a placeholder to establish the project structure.

# Example with SQLModel (to be implemented later):
# from sqlmodel import SQLModel, Field
#
# class User(SQLModel, table=True):
#     id: int | None = Field(default=None, primary_key=True)
#     telegram_id: int = Field(unique=True, index=True)
#     first_name: str
#     last_name: str | None = None
#     username: str | None = None
#     is_admin: bool = False
